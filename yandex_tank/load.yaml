overload:
  enabled: true 
  package: yandextank.plugins.DataUploader
  token_file: "token.txt"
  job_name: test

phantom:
  address: nginx
  load_profile:
    load_type: rps
    schedule: step(50, 100, 10, 5) 
  writelog: all
  uris:
    - "/fac/10" 
    - "/io/10" 
console:
  enabled: true
telegraf:
  enabled: false 
